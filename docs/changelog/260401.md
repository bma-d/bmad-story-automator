# 260401

## Added

- Created the standalone `bmad-story-automator` package repo from the Python automator source.
- Mirrored the shared bundle structure used by `bmad-story-automator-go`: npm entrypoint, installer, payload, source, docs.

## Changed

- Renamed the install surface from `story-automator-py` to `story-automator`.
- Renamed the Claude entry command to `/bmad-bmm-story-automator`.
- Installer now migrates current-layout installs toward `bmad-story-automator` and removes the old `-py` command shim.

## 260401-22:47:02 - Prepare repository for open source release

### Summary
Added public-repo metadata, support docs, CI verification, and install smoke coverage for the Python port.

### Added
- Added MIT license files for the repo and bundled Python package.
- Added contributor/security docs, CODEOWNERS, and a bug report issue template.
- Added CI and a smoke-test harness covering current and legacy BMAD layouts.

### Changed
- Updated README to document the Go source repo and the lower test confidence of the Python port.
- Added package metadata and verification scripts for npm and Python release flows.

### Removed
- Removed the temporary open-source checklist document from the repository.

### Files
- `README.md`
- `package.json`
- `source/pyproject.toml`
- `LICENSE`
- `source/LICENSE`
- `CONTRIBUTING.md`
- `SECURITY.md`
- `.github/CODEOWNERS`
- `.github/ISSUE_TEMPLATE/bug_report.yml`
- `.github/workflows/ci.yml`
- `scripts/smoke-test.sh`
- `source/README.md`
- `.gitignore`

### QA Notes
- N/A

## 260414-10:30:57 - Harden tmux runtime for non-default shells and update BMAD workflow path resolution

### Summary
Reworked the tmux execution runtime to stop depending on the tmux server's interactive shell behavior, then followed up with installer and workflow-path updates so modern BMAD skill-based installs resolve dependency workflows from the new `.claude/skills` and `.agents/skills` locations.

### Added
- Added a shared tmux runtime module with explicit session state, runner ownership, and tests for terminal lifecycle behavior.
- Added smoke coverage for modern BMAD skill-only dependency layouts in addition to current and legacy `_bmad` workflow layouts.

### Changed
- Replaced the older shell-dependent tmux spawning path with a runner-based runtime compatible with non-default shells and customized tmux defaults.
- Simplified `commands/tmux.py` and `adapters/tmux.py` to use the shared tmux runtime instead of duplicating lifecycle logic.
- Updated workflow dependency resolution so Claude wrappers prefer `.claude/skills`, Codex prompts prefer `.agents/skills`, and both fall back to current and legacy `_bmad` locations.
- Updated packaged workflow guidance and README install notes to document the new BMAD path resolution behavior.

### Files
- `source/src/story_automator/core/tmux_runtime.py`
- `source/src/story_automator/commands/tmux.py`
- `source/src/story_automator/adapters/tmux.py`
- `source/tests/test_tmux_runtime.py`
- `payload/_bmad/bmm/workflows/4-implementation/story-automator/data/tmux-commands.md`
- `install.sh`
- `source/src/story_automator/core/workflow_paths.py`
- `scripts/smoke-test.sh`
- `payload/_bmad/bmm/workflows/4-implementation/story-automator/data/workflow-commands.md`
- `README.md`

### QA Notes
- `python3 -m compileall source/src`
- `PYTHONPATH=source/src python3 -m unittest source.tests.test_tmux_runtime`
- `npm run test:smoke`

## 260414-12:07:46 - Fix Claude runner sessions that stay open at the prompt after command completion

### Summary
Fixed a runner-mode regression where Claude command sessions could finish their work, return to the interactive prompt, and still remain marked `running`, causing `monitor-session` to hang instead of advancing the orchestrator.

### Changed
- Updated runner-mode status detection to treat Claude sessions as complete when the pane shows the idle prompt together with a completion marker, even if the Claude process remains open interactively.
- Relaxed prompt detection so Claude's prompt is recognized when it appears above the status panel rather than only on the final captured line.

### Files
- `source/src/story_automator/core/tmux_runtime.py`
- `source/tests/test_tmux_runtime.py`

### QA Notes
- `PYTHONPATH=source/src python3 -m unittest source.tests.test_tmux_runtime`
- `python3 -m compileall source/src`
