# 260401

## 260412 - Pure Skill Install Layout

### Changed
- Installer now writes story-automator and story-automator-review under `.claude/skills`.
- Bundled Python helper runtime now installs inside `bmad-story-automator/scripts` with source under the same skill tree.
- Runtime workflow resolution now targets pure BMAD dependency skills under `.claude/skills`.
- Smoke tests now build pure-skill fixtures and verify command-wrapper-free installs.

### Removed
- Removed `_bmad/bmm` workflow roots as primary story-automator install targets.
- Stopped generating Claude command wrappers for story-automator installs.

## Added

- Created the standalone `bmad-story-automator` package repo from the Python automator source.
- Mirrored the shared bundle structure used by `bmad-story-automator-go`: npm entrypoint, installer, payload, source, docs.

## Changed

- Renamed the install surface from `story-automator-py` to `story-automator`.
- Renamed the Claude entry command to `/bmad-bmm-story-automator`.
- Installer now migrates current-layout installs toward `bmad-story-automator` and removes the old `-py` command shim.

## 260401-22:47:02 - Prepare repository for open source release

### Summary
Added public-repo metadata, support docs, CI verification, and install smoke coverage for the Python port.

### Added
- Added MIT license files for the repo and bundled Python package.
- Added contributor/security docs, CODEOWNERS, and a bug report issue template.
- Added CI and a smoke-test harness covering current and legacy BMAD layouts.

### Changed
- Updated README to document the Go source repo and the lower test confidence of the Python port.
- Added package metadata and verification scripts for npm and Python release flows.

### Removed
- Removed the temporary open-source checklist document from the repository.

### Files
- `README.md`
- `package.json`
- `source/pyproject.toml`
- `LICENSE`
- `source/LICENSE`
- `CONTRIBUTING.md`
- `SECURITY.md`
- `.github/CODEOWNERS`
- `.github/ISSUE_TEMPLATE/bug_report.yml`
- `.github/workflows/ci.yml`
- `scripts/smoke-test.sh`
- `source/README.md`
- `.gitignore`

### QA Notes
- N/A
