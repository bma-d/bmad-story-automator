# Changelog - 260413

## 260413-11:35:00 - Verify packed npx install path

### Summary
Closed the release gap that let npm ship stale legacy payloads by verifying the real packed tarball install path.

### Changed
- Changed smoke coverage to `npm pack` the repo first, then install via `npx --package file:<tarball>` so release verification matches the publish artifact.
- Updated release notes to route npm auth material through [$secrets](/Users/joon/.agents/skills/secrets/SKILL.md) instead of ad hoc token handling.
- Bumped the package/runtime version to `1.13.0` for the republish.

### Files
- `scripts/smoke-test.sh`
- `docs/development.md`
- `package.json`
- `source/pyproject.toml`

### QA Notes
- `npm run verify`

## 260413-03:41:50 - Stabilize Codex tmux review sessions

### Summary
Fixed false `never_active` Codex review failures and quieted Codex child-session startup noise.

### Changed
- Changed Codex tmux commands to run with an isolated `/tmp` `CODEX_HOME` that links auth only, keeps `approval_policy="never"`, preserves high reasoning, and disables noisy plugin/sqlite/shell-snapshot startup paths.
- Updated smoke coverage to assert the new isolated Codex launch contract.

### Fixed
- Fixed tmux monitoring to find nested Codex and Claude child processes instead of checking direct children only.
- Fixed Codex session status handling so idle child processes stay `in_progress` instead of being marked `never_active`.

### Files
- `source/src/story_automator/commands/tmux.py`
- `scripts/smoke-test.sh`

### QA Notes
- N/A

## 260413-05:03:47 - Add comprehensive automator documentation

### Summary
Reworked the package docs into a multi-file operator guide covering flow, runtime, state, review, and troubleshooting.

### Added
- Added a dedicated docs set for execution flow, state/resume behavior, agent monitoring, installation, review semantics, CLI usage, troubleshooting, and development.
- Added Mermaid diagrams across the root README and supporting docs to mirror the Go version's operator-facing style.

### Changed
- Rewrote the root README to act as the documentation entrypoint instead of a narrow install note.

### Files
- `README.md`
- `docs/how-it-works.md`
- `docs/story-execution.md`
- `docs/state-and-resume.md`
- `docs/agents-and-monitoring.md`
- `docs/installation-and-layout.md`
- `docs/review-workflow.md`
- `docs/cli-reference.md`
- `docs/troubleshooting.md`
- `docs/development.md`

### QA Notes
- N/A
